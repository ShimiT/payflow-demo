apiVersion: v1
kind: Service
metadata:
  name: {{ include "payflow.fullname" . }}-backend
  labels:
    {{- include "payflow.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payflow.backend.selectorLabels" . | nindent 4 }}
